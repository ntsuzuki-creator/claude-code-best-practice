# Claude Code ベストプラクティス ハンズオン

このドキュメントは、シンプルな TODO 管理 CLI（`todo.py` / `cli.py` / `tests/test_todo.py`）を題材に、
Claude Code の公式ベストプラクティスを実際に手を動かしながら身につけるための教材です。

各モジュールは次の4点セットで構成されています。

- **ゴール**: このモジュールで体験すること
- **手順**: 実際に行う作業
- **Claude Code への依頼例**: そのままチャットに貼れるプロンプト例
- **確認方法**: うまくいったかどうかのチェックポイント

モジュールは順番に進めることを推奨しますが、興味のあるものから拾い読みしても構いません。

---

## Module 0: 準備運動

**ゴール**: 環境を整え、CLAUDE.md が自動的に読み込まれていることを確認する。

**手順**:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pytest
```

`python cli.py add "牛乳を買う"` → `python cli.py list` を実行し、動作を確認しておきましょう。

**Claude Code への依頼例**:

```
このプロジェクトの CLAUDE.md の内容を要約して。今どんなルールが適用されている？
```

**確認方法**: Claude が `todo.py` / `cli.py` / `tests/test_todo.py` の役割分担や、テストコマンドを
CLAUDE.md の記述通りに説明できれば、プロジェクトコンテキストが自動的に読み込まれています。

---

## Module 1: CLAUDE.md を育てる

**ゴール**: プロジェクト固有のルールを CLAUDE.md に追記すると、Claude の振る舞いがどう変わるかを体験する。

**手順**:

1. まずは追記前の状態で、次の依頼例をそのまま試す。
2. CLAUDE.md の末尾に以下のようなルールを追記する。

   ```markdown
   ## 開発ルール

   - 新しい関数を追加するときは、必ず対応するテストも同時に追加すること
   - コミットメッセージは日本語で書くこと
   ```

3. 同じ依頼例をもう一度試し、挙動の違いを比較する。

**Claude Code への依頼例**:

```
todo.py に、完了済みタスクの数を返す count_done() という関数を追加して。
```

**確認方法**: ルール追記後は、実装と同時に `tests/test_todo.py` にテストケースが追加されるか
確認しましょう。追記前は追加されない（または依頼しないと追加されない）ことが多いはずです。
この差分が CLAUDE.md の効果です。

---

## Module 2: Plan Mode で機能追加サイクルを回す

**ゴール**: 「調査 → 計画 → 承認 → 実装」という Plan Mode のワークフローを体験する。

**手順**:

1. Claude Code で `shift+tab` を押して Plan Mode に切り替える（プロンプト欄に `plan mode on` 等の表示が出ます）。
2. 下記の依頼例を送る。
3. Claude が `todo.py` / `cli.py` / テストへの変更方針を「計画」として提示するので、内容を確認してから承認する。
4. 承認後、実際にコードが変更されるのを確認する。

**Claude Code への依頼例**:

```
タスクに優先度(priority: high / medium / low、デフォルトは medium)を追加したい。
add コマンドでオプションとして指定でき、list コマンドの表示にも反映されるようにして。
```

**確認方法**:

- 計画を承認する前に、変更対象ファイルとテスト方針が明示されているか確認する
- 実装後、`pytest` が通ることを確認する
- `python cli.py add "資料作成" --priority high` のようにオプションが使えることを確認する

---

## Module 3: TDD 的な検証フロー

**ゴール**: 「先にテストを書かせる → red を確認 → 実装させて green にする」という
Anthropic 推奨の TDD ワークフローを体験する。

**手順**:

1. Module 2 で追加した priority 機能について、まずはテストだけを書いてもらう（実装はしないよう明示する）。
2. `pytest` を実行し、red（失敗）になることを確認する。
3. 実装を依頼し、`pytest` が green（成功）になることを確認する。

**Claude Code への依頼例（1回目・テストのみ）**:

```
add_task に不正な priority（"urgent" など）を渡した場合に ValueError を送出してほしい。
まずはこの仕様を検証する pytest のテストケースだけを tests/test_todo.py に追加して。
実装はまだ変更しないで。
```

**Claude Code への依頼例（2回目・実装）**:

```
さっき追加したテストが通るように、add_task に priority のバリデーションを実装して。
```

**確認方法**: 1回目の後に `pytest` を実行すると新しいテストが失敗する（red）こと、
2回目の後に成功する（green）ことを、自分の目で確認するのがこのモジュールの学習ポイントです。

---

## Module 4: バグ修正ワークフロー

**ゴール**: 既存バグの発見・再現・修正・再発防止テストという一連の流れを体験する。

**手順**:

1. 現在のブランチ上で、意図的に小さなバグを仕込む。例えば `todo.py` の
   `remove_task` の比較条件 `task["id"] != task_id` を `task["id"] == task_id` のように壊してみる
   （挙動が逆転し、削除したいタスク以外が消えるバグになります）。
2. 新しい Claude Code セッション（または新しい会話）を開始し、バグの「症状」だけを伝える
   （原因やコード箇所は伝えない）。
3. Claude に原因調査・修正・再発防止テストの追加までを依頼する。

**Claude Code への依頼例**:

```
python cli.py remove 2 を実行すると、期待と違ってタスク2以外が消えてしまいます。
原因を調査して直してください。再発防止のテストも追加してください。
```

**確認方法**:

- 修正前に `pytest` を実行し、既存テストが失敗する（バグが再現している）ことを確認する
- 修正後に `pytest` が全て通ることを確認する
- 新しく追加されたテストが、今回のバグを再現するケースになっているか確認する

---

## Module 5: カスタムスラッシュコマンドを作る

**ゴール**: `.claude/commands/` にプロジェクト専用コマンドを自作し、繰り返し作業を効率化する。

このリポジトリには実例として `.claude/commands/add-task-with-test.md` を用意しています。
これは「タスク追加の動作確認 + 対応するテストケースの雛形生成」をまとめて依頼するコマンドです。

**手順**:

1. Claude Code で `/add-task-with-test 資料を作成する` のように実行してみる。
2. `.claude/commands/add-task-with-test.md` の中身を開き、どう書かれているか確認する。
3. 自分で別のカスタムコマンド（例: `/list-summary` でタスク一覧を要約する）を作ってみる。

**Claude Code への依頼例（コマンド自作を手伝ってもらう場合）**:

```
.claude/commands/ に list-summary というカスタムコマンドを作りたい。
実行すると、現在のタスク一覧を集計して「未完了n件・完了n件」のサマリを表示してほしい。
```

**確認方法**: Claude Code の `/` メニューにコマンドが一覧表示され、実行できることを確認する。

---

## Module 6: サブエージェントを使う

**ゴール**: 組み込みの `Explore` エージェントと、プロジェクト固有のサブエージェント
（`.claude/agents/`）の使い分けを理解する。

このリポジトリには実例として `.claude/agents/test-coverage-reviewer.md` を用意しています。
テストカバレッジの不足箇所（例: `cli.py` が未テストであること）を指摘する専用サブエージェントです。

**手順**:

1. Claude Code に「test-coverage-reviewer サブエージェントで tests/ をレビューして」のように依頼する。
2. 独立した文脈でレポートが返ってくることを確認する。
3. 組み込みの `Explore` エージェントと結果や使い勝手を比べてみる
   （例: 「Explore エージェントで todo.py の全関数を洗い出して」）。

**Claude Code への依頼例**:

```
test-coverage-reviewer サブエージェントを使って、tests/test_todo.py のカバレッジ不足を指摘して。
```

**確認方法**: サブエージェントが呼び出され、メインの会話とは別の文脈で結果（レポート）だけが
返ってくることを確認する。

---

## まとめ・振り返り

このハンズオンで体験したベストプラクティス:

- **CLAUDE.md**: プロジェクト固有のルール・コンテキストを自動的に Claude に伝える仕組み（Module 1）
- **Plan Mode**: 大きめの変更の前に「調査 → 計画 → 承認 → 実装」のサイクルを踏む（Module 2）
- **TDD ワークフロー**: テストを先に書かせて red → green を確認しながら進める（Module 3）
- **バグ修正ワークフロー**: 症状だけを伝えて調査・修正・再発防止テストを一貫して任せる（Module 4）
- **カスタムスラッシュコマンド**: `.claude/commands/` で繰り返し作業をコマンド化する（Module 5）
- **サブエージェント**: `.claude/agents/` で役割の異なる専用エージェントを定義し、文脈を分離する（Module 6）

### 発展編（今回スコープ外）

余裕があれば次のトピックにも挑戦してみましょう。

- **Hooks**: `settings.json` でツール実行前後に自動処理を挟む仕組み
- **MCP**: 外部サービス・データソースと連携するための Model Context Protocol
- **コードレビュー系スキル**: `/code-review` や `/simplify` を使った品質改善ワークフロー
